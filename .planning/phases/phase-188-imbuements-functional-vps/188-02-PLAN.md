# Plan 188-02: Ajustes de integração no GamePrototype.tsx, BottomDock.tsx e verificação Vitest

## Objective
Garantir que a abertura do modal `Imbuir` via clique no botão `IMBUEMENTS` na barra de ações rápidas (`BottomDock.tsx` e `QuickActionDock.tsx`) funcione em 100% dos cenários (com ou sem caçada ativa, na cidade e durante a visualização de qualquer herói da party), com testes automatizados passando.

## Technical Steps
1. Revisar `BottomDock.tsx`:
   - Garantir que o botão `IMBUEMENTS` possui `onClick={onOpenImbuements}` ativo e estilizado.
   - Também atualizar `QuickActionDock.tsx` se utilizado em algum ponto.
2. Revisar `GamePrototype.tsx`:
   - Verificar handlers `onImbueItem`, `onClearSlot`, `onToggleAutoRenew` e garantir que o estado `imbuingModalOpen` responde instantaneamente.
3. Adicionar testes em `tests/phase187-imbuements-system.test.ts` validando a resolução de slots nativa do `equipmentCatalog` e a consistência do catálogo.
4. Executar Vitest e `npm run typecheck` com 0 erros.
