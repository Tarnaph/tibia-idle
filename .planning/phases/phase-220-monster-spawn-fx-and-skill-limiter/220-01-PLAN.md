# Plan: Phase 220 - Monster Spawn Teleport FX & Skill Rate Limiter Hunt Fix

## Context & Objectives
1. **Teleport FX no Spawn de Monstros (`CONST_ME_TELEPORT` / effect 11):**
   - Eliminar o spawn "seco": ao materializar ou renascer criaturas em zonas contínuas ou pulls ao redor da party, emitir evento visual autoritativo `spawn-visual` com `effectId: 11` na posição do monstro.
   - Renderizar no PixiJS (`PixiArena.tsx`) a animação de 11 frames do portal/turbilhão azul clássico do Tibia sobre o monstro que acaba de surgir.
2. **Correção do Erro de Rate Limiter de Treino/Habilidade ("Salto anômalo de habilidade não permitido"):**
   - Corrigir a validação de skills em `packages/auth/src/characterService.ts` para herdar `leaderContext` quando `data.leaderCharacterId` é fornecido (idêntico ao que foi feito para XP na Phase 217), garantindo que alts da party (como o Cerberus) sejam reconhecidos com `isHunting = true`.
   - Calibrar as taxas de `SkillRateLimiter.ts`: `HUNT_MAX_TRIES_PER_SECOND` de 9.000 para 40.000 e `HUNT_MAX_BURST_TRIES` de 225.000 para 600.000, acomodando legitimamente a rotação contínua de magias aceleradas por mana potions e multiplicadas pelas rates do servidor (`rateMagic = 25` + stage `10x` = 250x).
   - Prevenir a bola de neve de erros 400 consecutivos entre requisições de salvamento.
3. **Testes Automatizados & Deploy:**
   - 0 erros de tipagem TypeScript (`tsc`).
   - 100% de aprovação nos testes automatizados (`vitest`).
   - Deploy completo em produção na VPS (`187.7.16.210`).
