# 123-01-PLAN: Correção de Addons Invisíveis, Normalização Canônica de Outfits (Noble/Noblewoman), Hidratação de Estado e Persistência Permanente

## 1. Escopo de Arquivos e Componentes
- `apps/web/lib/outfitRecolor.ts`:
  - Mapear aliases `noble`, `nobleman`, `noblewoman` para o ID canônico `noblewoman` em `normalizeOutfitId`.
  - Atualizar `CANONICAL_OUTFITS.find` para resolver `Noble` / `Nobleman` para `noblewoman`.
  - Proteger `recoloredCanvasCache` contra poluição: se qualquer camada solicitada falhou no carregamento, salvar apenas no `provisionalCanvasCache`, reservando o `recoloredCanvasCache` para composições 100% íntegras.
- `packages/auth/src/characterService.ts`:
  - Em `saveCharacterProgress`, aceitar e atualizar `outfit`, `outfitHead`, `outfitBody`, `outfitLegs`, `outfitFeet`, `outfitAddons`, `mount`, `mountActive`.
- `app/api/characters/[id]/save/route.ts`:
  - Extrair campos visuais do `body` e repassar para `saveCharacterProgress`.
- `app/api/characters/route.ts`:
  - Retornar `addons: c.outfitAddons ?? 0` e `outfitAddons: c.outfitAddons ?? 0` na listagem e na criação de personagens.
- `apps/web/components/GamePrototype.tsx`:
  - Na hidratação inicial de `userChar` (linhas 1060-1120), definir `userChar.addons`, `userChar.mount`, `userChar.mountActive`.
  - Passar `addons: userChar.addons` nas opções de `gameNetwork.connect`.
  - No callback `handleSaveOutfit`, enviar requisição POST imediata para `/api/characters/${characterId}/save` persistindo `outfit`, `addons`, `mount`, `mountActive`, `outfitColors`.
  - Incluir esses campos no autosave periódico (a cada 5s) e `beforeunload`.
- `apps/web/components/PixiArena.tsx`:
  - Usar fallback seguro para addons: `const addons = (character as any).addons ?? (character as any).outfitAddons ?? 0;`.
- `apps/web/components/ThaisCityArena.tsx`:
  - Na função `ensureActorView`, passar `addons`, `char.mount`, `isMounted` na chamada inicial a `getRecoloredCanvasSync`.
- `apps/web/components/OutfitModal.tsx`:
  - No filtro `filterAcquired`, respeitar `activeChar?.isPremium !== false`.
- `tests/phase44-functional-outfit-recolor-system.test.ts`:
  - Atualizar teste de normalização de 'Noble' para o valor canônico 'noblewoman'.
- `tests/phase123-outfit-addons-persistence-canonical-fix.test.ts`:
  - Suíte de testes unitários e de integração cobrindo todas as correções.

## 2. Ordem de Execução
1. Modificar `apps/web/lib/outfitRecolor.ts`.
2. Atualizar `packages/auth/src/characterService.ts`, `app/api/characters/[id]/save/route.ts`, `app/api/characters/route.ts`.
3. Atualizar `apps/web/components/GamePrototype.tsx`, `PixiArena.tsx`, `ThaisCityArena.tsx`, `OutfitModal.tsx`.
4. Atualizar `tests/phase44-functional-outfit-recolor-system.test.ts`.
5. Criar `tests/phase123-outfit-addons-persistence-canonical-fix.test.ts`.
6. Rodar `npm run typecheck` e `npm run test`.
7. Criar `123-SUMMARY.md` e atualizar status no `ROADMAP.md` e `STATE.md`.
