# Phase 62 Plan: Remoção do Sistema de Trade entre Personagens

## Goal
Remover o sistema de trocas diretas (Trade) entre personagens na cidade, limpando a opção do menu de contexto, o registro de janela no `WindowManagerContext`, a renderização do `<TradeWindow>` e os handlers de estado de trocas no frontend.

## Proposed Changes

### 1. Menu de Contexto (Frontend)
#### [MODIFY] `apps/web/components/CharacterContextMenu.tsx`
- Remover o botão `🤝 Trade (Trocar Itens)` e a prop `onTrade` do menu de contexto.

### 2. Gerenciador de Janelas (`WindowManagerContext.tsx`)
#### [MODIFY] `apps/web/components/window/WindowManagerContext.tsx`
- Remover o ID `'trade'` do tipo `WindowId` e da lista inicial de janelas.

### 3. Componente de Jogo Principal (`GamePrototype.tsx`)
#### [MODIFY] `apps/web/components/GamePrototype.tsx`
- Remover a importação de `TradeWindow`, o estado `tradeSession` e os handlers `handleStartTrade`, `handleOfferItem`, `handleRemoveOffer`, `handleAcceptTrade`, `handleCancelTrade`.

### 4. Testes Automatizados (`tests`)
#### [NEW] `tests/phase62-remove-trade-system.test.ts`
- Teste no Vitest garantindo que a janela e os handlers de trade foram totalmente limpos sem quebrar outros sistemas de interface.

## Verification Plan
1. Rodar `npm run typecheck` e garantir 0 erros de compilação.
2. Rodar `npx vitest run tests/phase62-remove-trade-system.test.ts`.
