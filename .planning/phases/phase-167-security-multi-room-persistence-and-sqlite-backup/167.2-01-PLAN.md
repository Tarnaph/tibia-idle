# Phase 167.2 Plan: Bloco 1.1b - Correções Críticas de Migração SQLite, Reconciliação Integral 409, Contexto Server-Side de Caçada e Deduplicação de Orçamento de XP

## Objetivo
Corrigir com rigor técnico os 4 pontos de homologação levantados antes do avanço para a Party:
1. Adaptar a migração SQLite para usar o padrão `RedefineTables` que funciona com bancos já povoados (eliminando o erro `Cannot add a column with non-constant default`), testar com dados existentes e documentar `prisma migrate resolve --applied` para bancos que receberam os campos via `db push`.
2. Corrigir a reconciliação do HTTP 409 no `GamePrototype.tsx` para atualizar todo o inventário real (`gold`, `bag` e `loot`) e mapear skills por `skillId` e chaves compatíveis com `Sword Fighting`, garantindo que o próximo autosave utilize o estado reconciliado sem retornar valores antigos.
3. Desacoplar a validação de caçada da API de flags ingênuas enviadas pelo cliente; consultar o contexto autoritativo registrado pelo servidor (`ServerCharacterContextRegistry`), avaliando salvamentos legítimos com o orçamento correto.
4. Eliminar a dupla cobrança do orçamento de XP entre WebSocket e persistência (`PrismaPersistenceManager` / `CharacterService`), garantindo que cada ganho seja contabilizado uma única vez e revertido (`refund`) em falhas/conflitos.
5. Criar suíte de testes automatizados com SQLite cobrindo os 4 cenários específicos e executando os dois serviços reais (`CharacterService` e `PrismaPersistenceManager`) disputando concorrência atômica.
6. Manter o rastreamento da origem dos itens documentado como limitação de homologação até o bloco de economia/trade.

## Tarefas de Execução
- [ ] 1. Atualizar a migração `prisma/migrations/20260914120000_add_save_version_and_last_saved_at/migration.sql` com o padrão SQLite `RedefineTables`.
- [ ] 2. Criar `packages/auth/src/characterContextRegistry.ts` e exportar em `packages/auth/src/index.ts`.
- [ ] 3. Atualizar `packages/server/src/rooms/ThaisCityRoom.ts` para registrar contexto de caçada no registry server-side.
- [ ] 4. Atualizar `packages/auth/src/xpRateLimiter.ts` com `recordAuthorizedExp`, `getAuthorizedExp` e `refund`.
- [ ] 5. Atualizar `packages/server/src/persistence/PrismaPersistenceManager.ts` para não debitar XP já autorizado no WebSocket e refund em rollback.
- [ ] 6. Atualizar `packages/auth/src/characterService.ts` para consultar server context registry e efetuar refund em falhas/409.
- [ ] 7. Atualizar `apps/web/components/GamePrototype.tsx` para reconciliar integralmente `gold`, `bag`, `loot` e skills mapeadas por `skillId`.
- [ ] 8. Criar suíte de testes `tests/block1-1b-concurrency-and-xp-reconciliation.test.ts` cobrindo todos os cenários.
- [ ] 9. Executar verificação contínua (`npm run typecheck` e `npx vitest run`).
- [ ] 10. Documentar orientações de migração e limitação de homologação, e gerar resumo `167.2-SUMMARY.md`.
